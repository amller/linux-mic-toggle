# linux-mic-toggle
## GNOME Setup
1. Open Settings and go to Keyboard -> Keyboard Shortcuts -> View and Customize Shortcuts -> Custom Shortcuts
2. Create a new Shortcut which executes following command: `sh <absolute path to cloned repo>/mic-toggle.sh`
